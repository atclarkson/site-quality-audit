{
  "name": "@site-quality-audit/queue",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node20 --dts --out-dir dist --clean",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "bullmq": "^5.58.0",
    "ioredis": "^5.7.0",
    "zod": "^4.0.0"
  }
}
